version: 2

sources:
  - name: raw_contracts
    description: Raw contract data from seeds (sample data)
    database: contracts_dw
    schema: public
    tables:
      - name: contracts
        description: Contract header information
      - name: rate_schedules
        description: Rate schedule data from contracts
      - name: amendments
        description: Contract amendments
